<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.minipaintdex</groupId>
  <artifactId>minipaintdex-parent</artifactId>
  <version>0.2.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>MiniPaintDex</name>

  <modules>
    <module>backend/domain</module>
    <module>backend/application</module>
    <module>backend/adapter-file</module>
    <module>backend/adapter-spring-events</module>
    <module>backend/bootstrap</module>
    <module>backend/server</module>
    <module>backend/cli</module>
  </modules>

  <properties>
    <java.version>25</java.version>
    <maven.compiler.release>25</maven.compiler.release>
    <picocli.version>4.7.7</picocli.version>
    <springdoc.version>3.1.0</springdoc.version>
    <archunit.version>1.4.1</archunit.version>
    <jacoco.version>0.8.14</jacoco.version>
    <frontend-maven-plugin.version>2.0.1</frontend-maven-plugin.version>
    <node.version>v24.11.1</node.version>
    <pnpm.version>11.19.0</pnpm.version>
    <frontend.skip>false</frontend.skip>
    <python.executable>python</python.executable>
    <data-tools.skip>false</data-tools.skip>
    <jacoco.line.minimum>0.00</jacoco.line.minimum>
    <jacoco.branch.minimum>0.00</jacoco.branch.minimum>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>info.picocli</groupId>
        <artifactId>picocli</artifactId>
        <version>${picocli.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc.version}</version>
      </dependency>
      <dependency>
        <groupId>com.tngtech.archunit</groupId>
        <artifactId>archunit-junit5</artifactId>
        <version>${archunit.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-code-coverage</id>
            <goals><goal>prepare-agent</goal></goals>
          </execution>
          <execution>
            <id>report-code-coverage</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
          </execution>
          <execution>
            <id>check-code-coverage</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.line.minimum}</minimum>
                    </limit>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.branch.minimum}</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-toolchain</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion><version>[25,26)</version></requireJavaVersion>
                <requireMavenVersion><version>[3.9.16,4)</version></requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>${frontend-maven-plugin.version}</version>
        <inherited>false</inherited>
        <configuration>
          <workingDirectory>${maven.multiModuleProjectDirectory}/frontend</workingDirectory>
          <installDirectory>${project.build.directory}/toolchain</installDirectory>
          <skip>${frontend.skip}</skip>
          <environmentVariables>
            <CI>true</CI>
          </environmentVariables>
        </configuration>
        <executions>
          <execution>
            <id>install-node-and-pnpm</id>
            <phase>generate-resources</phase>
            <goals><goal>install-node-and-pnpm</goal></goals>
            <configuration>
              <nodeVersion>${node.version}</nodeVersion>
              <pnpmVersion>${pnpm.version}</pnpmVersion>
            </configuration>
          </execution>
          <execution>
            <id>pnpm-install</id>
            <phase>generate-resources</phase>
            <goals><goal>pnpm</goal></goals>
            <configuration><arguments>install --frozen-lockfile</arguments></configuration>
          </execution>
          <execution>
            <id>frontend-check</id>
            <phase>verify</phase>
            <goals><goal>pnpm</goal></goals>
            <configuration><arguments>run check:web</arguments></configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <inherited>false</inherited>
        <executions>
          <execution>
            <id>data-tools-test</id>
            <phase>test</phase>
            <goals><goal>exec</goal></goals>
            <configuration>
              <skip>${data-tools.skip}</skip>
              <executable>${python.executable}</executable>
              <workingDirectory>${maven.multiModuleProjectDirectory}</workingDirectory>
              <environmentVariables>
                <PYTHONPATH>${maven.multiModuleProjectDirectory}/tools/minipaintdex-data/src</PYTHONPATH>
              </environmentVariables>
              <arguments>
                <argument>-m</argument>
                <argument>unittest</argument>
                <argument>discover</argument>
                <argument>-s</argument>
                <argument>${maven.multiModuleProjectDirectory}/tools/minipaintdex-data/tests</argument>
                <argument>-v</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
